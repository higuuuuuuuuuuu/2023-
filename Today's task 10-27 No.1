#include <stdio.h>

int main(void)
{
	int a, b, c, result;

	printf("数字を入力してください");
	scanf_s("%d",&a);
	printf("数字を入力してください");
	scanf_s("%d",&b);

	c = a * b;
	printf("入力された数字同士を掛け合わせると値は%d*%d=%d\nになる",a,b,c);

}
